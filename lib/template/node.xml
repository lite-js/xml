<% var attrs = lang.keys(attributes); %>
<<%= tag %><% if (attrs.length) { %> <%= ' ' + lang.map(attrs, function (attr) { return attr + '="' + attributes[attr] + '"'; }).join(' ') %><% } %>>
  <%= content %>
</<%= tag %>>
